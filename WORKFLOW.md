# 三段流转与点火机制

> **版本**：v0.1-stage1-3  
> **冻结日期**：2026-05-25  
> **依赖**：本文档以 CONSTITUTION.md 和 BOUNDARIES.md 为上位约束。

---

## 三段关系

```
人类（Human）
  ↕ 意图·裁决·价值锚点
Claude 路线（多层递归模型）
  ↕ 结构·协议·边界定义
OpenAI 路线（单层高效模型）
  ↕ 执行·产物·短链闭环
人类（Human）← 回收·确认·下一轮启动
```

### 人类层职责
- 提供目标、价值、终局裁决。
- 解释失败的权利与义务。
- 决定何时切换路线、何时冻结、何时进入下一阶段。

### Claude 路线职责
- 把人类意图压成结构与边界。
- 识别漂移并主动收束。
- 承接复杂建模、失败吸收、协议化。

### OpenAI 路线职责
- 把结构压成动作与产物。
- 快速执行、批量传导、局部修补。
- 不自行扩展上层结构。

---

## 流转顺序

```
1. 人类定意图
2. Claude 压成结构与边界
3. OpenAI 压成动作与产物
4. 结果回流人类
5. 人类确认 → 进入下一轮 / 冻结 / 回退
```

---

## 点火机制

点火不是"继续说"，而是**切换责任主体**。

### 触发条件（满足其一即可触发）

1. 当前问题已从抽象转入执行。
2. 当前问题已从执行转入复核。
3. 当前问题已出现歧义、冲突或失败征兆。
4. 当前问题需要从长链路切换到短链路。
5. 当前模型出现扩口或漂移行为。

### 点火规则

- 只有明确触发词才允许切换路线。
- 每次切换必须记录理由。
- 每次执行结束必须回收状态。
- 不允许在同一轮里同时展开两个以上阶段。
- 每轮结束必须回到冻结锚点确认。

### 触发词参考

| 触发词 | 切换方向 |
|--------|----------|
| "开始执行" | → OpenAI 路线 |
| "需要建模" | → Claude 路线 |
| "回到总宪法" | → 人类 + Claude |
| "冻结当前阶段" | → 状态存档 |
| "进入下一阶段" | → 人类裁决后解锁 |
| "识别到漂移" | → Claude 收束 |

---

## 冻结锚点

> **总宪法定根原则，Claude 定递归边界，OpenAI 定传导边界，人负责终局裁决，点火机制负责切换责任。**

---

*本文档属于流转层，修改触发词需记录变更原因。*
