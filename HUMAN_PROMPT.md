# 人类一侧全周期使用提示词文档

> 版本：v0.1 | 冻结日期：2026-05-25  
> 适用范围：与R路线模型（Claude系列）及T路线模型（OpenAI系列）的全周期协作

---

## 核心使用原则

你不是在"跟AI聊天"，你是在**操控一个有分工、有边界、有记忆协议的协同系统**。  
你的语言不仅是表达意图，也是**点火触发器**：决定哪个路线被激活、哪个阶段被解锁、哪个产物被输出。

---

## 标准指令模板

每次启动任务，建议使用以下格式：

```
本轮阶段：[阶段编号，如：阶段3]
本轮目标：[只做一件事，用一句话描述]
输出载体：[文档 / 清单 / 表格 / 协议草案 / 架构图]
限制条件：[不扩展到哪个阶段，不讨论哪些话题]
收束锚点：[结束时回到哪句话 / 哪个状态]
```

### 示例

```
本轮阶段：阶段3
本轮目标：只输出三段流转顺序的表格
输出载体：一张Markdown表格
限制条件：不讨论点火机制，不扩展到阶段4
收束锚点：完成后回到"总宪法—双路线边界—三段流转"冻结锚点
```

---

## 路线切换指令

需要切换路线时，使用以下明确触发词：

| 场景 | 触发词 |
|------|--------|
| 从意图转入结构化 | "现在进入结构化，请压缩为协议" |
| 从结构转入执行 | "结构已确认，移交执行层" |
| 从执行转入复核 | "执行完成，请复核并回流" |
| 遇到分歧或失败 | "停止，回到人类裁决" |
| 阶段冻结 | "本阶段冻结，写入锚点" |
| 解锁下一阶段 | "解锁阶段[编号]，开始推进" |

---

## 防漂移指令

当你发现模型开始扩口、漂移、生成新层级时，使用以下指令立即收束：

- `"停止扩写，回到当前锚点"`
- `"只回答本轮目标，不要延伸"`
- `"压缩，不要补充"`
- `"这轮已完成，写入状态回收"`

---

## 全周期使用流程

```
阶段0：读取当前版本与冻结锚点
     ↓
阶段启动：使用标准指令模板发起任务
     ↓
执行中：每3轮检查是否漂移，必要时使用防漂移指令
     ↓
阶段收束：触发"本阶段冻结，写入锚点"
     ↓
状态回收：Agent输出本轮状态回收报告
     ↓
人类裁决：确认 / 修订 / 进入下一阶段
     ↓
进入下一阶段：使用"解锁阶段[编号]"触发
```

---

## 失败处理提示词

当输出质量不符合预期时：

```
这次输出不符合预期，具体原因是：[描述]
请不要重新生成，而是先回答：这次输出的结构性缺陷是什么？
然后再提出修订方案，等待我确认后再执行。
```

这样做可以让失败被学习，而不是被覆盖。

---

## 禁止使用的指令模式

- ❌ "继续" （无边界触发，容易引发漂移）
- ❌ "帮我完成所有内容" （无阶段约束）
- ❌ "你来决定怎么做" （放弃人类裁决权）
- ❌ "随便给我一个方案" （无输出载体约束）

---

## 速查卡

```
启动   → 使用标准指令模板
切换   → 使用明确触发词
防漂   → "停止扩写，回到当前锚点"
冻结   → "本阶段冻结，写入锚点"
失败   → 先问结构性缺陷，再修订，不直接重新生成
终局   → 人类裁决，不可下放
```
