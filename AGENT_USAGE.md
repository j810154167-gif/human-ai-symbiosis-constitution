# Agent使用说明·反馈格式·历史记录协议

> 版本：v0.1 | 冻结日期：2026-05-25

---

## Agent使用约束

使用本框架的任何Agent必须遵守以下约束：

1. **只做当前阶段的事**，不得自行解锁下一阶段
2. **只接受明确触发词作为点火信号**，不得顺滑地滑向新层级
3. **每轮结束必须回收状态**，写入当前阶段锚点
4. **遇到歧义·冲突·失败征兆必须停止**，等待人类裁决
5. **不得自行扩写治理结构**，任何新层级须经人类审议

---

## 启动协议

每次新会话启动时，Agent须执行以下步骤：

```
1. 读取当前版本号与阶段签注
2. 确认当前冻结锚点
3. 询问人类：本次任务属于哪个阶段？
4. 确认本轮目标、输出载体、限制条件
5. 开始执行
```

---

## 执行中提醒

- 每推进 3 轮，Agent须主动提示当前阶段和剩余约束
- 检测到以下信号时须主动停止并告知人类：
  - 话题扩口（讨论内容超出当前阶段边界）
  - 路线混淆（R路线任务被T路线承接，反之亦然）
  - 冻结点未确认就继续下钻
  - 连续3轮以上未回到锚点

---

## 结构化反馈格式

每轮执行结束，Agent须输出以下反馈结构：

```markdown
## 本轮状态回收
- 当前阶段：[阶段编号]
- 完成项：[本轮完成的内容]
- 未完成项：[本轮未处理的内容]
- 触发信号：[是否触发过点火·路线切换]
- 漂移警告：[是否检测到扩口·混淆·冻结违规]
- 下一步建议：[建议人类下一步操作]
- 锚点确认：[当前冻结锚点]
```

---

## 历史记录协议

- 每次冻结版本须生成 Git tag，格式：`v[版本]-[日期]`
- 每次变更须在 CHANGELOG 中记录（日期·触发原因·变更内容·影响范围）
- 跨会话任务须在新会话开始时先恢复上一次的状态回收记录
- 不允许静默覆盖或删除历史记录

---

## CHANGELOG格式

```markdown
## [版本号] - [日期]
### 新增
- 内容
### 修改
- 内容
### 删除
- 内容
### 触发原因
- 内容
### 影响范围
- 内容
```
